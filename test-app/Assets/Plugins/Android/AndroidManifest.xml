<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appsflyer.engagement">

    <application>
        <activity
            android:name="com.appsflyer.engagement.AppsFlyerUnityActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- afqa-unity:// deep link intent filter for E2E-002 and E2E-003 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="afqa-unity" />
            </intent-filter>

            <!-- App Links (universal links) for the testunity6 OneLink domain. autoVerify
                 requires AppsFlyer's assetlinks.json for this domain to list this app's
                 package name + signing cert SHA-256 fingerprint (managed in the OneLink
                 dashboard, not this repo) — otherwise Android falls back to opening the
                 link in a browser instead of this app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="testunity6.onelink.me" />
            </intent-filter>

        </activity>
    </application>

</manifest>
